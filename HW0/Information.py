# Hash table know as dictionary in python.

# Array can be used contruct of hash table using integer. But if we want to store something other than integer such as string then we must use as dictionary.
