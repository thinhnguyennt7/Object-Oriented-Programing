// The List interface extends the Collection interface and defines a 
// collection for storing elements in a sequential order. 
// To create a list, use one of its two concrete classes: ArrayList or LinkedList

//Functions for lists

// +add(index: int, element: Object): boolean 
// +addAll(index: int, c: Collection<? extends E>): boolean
// +get(index: int): E
// +indexOf(element: Object): int 
// +lastIndexOf(element: Object): int 
// +listIterator(): ListIterator<E> 
// +listIterator(startIndex: int): ListIterator<E> 
// +remove(index: int): E
// +set(index: int, element: Object): Object 
// +subList(fromIndex: int, toIndex: int): List<E>

// Should use iterator for linkedlist to traversal